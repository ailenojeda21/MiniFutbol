<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro de Usuarios</title> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/Estilo/registrarcli.css">
</head>
<body>
    <div class="titulo-header text-center">
        <div class="logo-container p-3">
  <img src="/Estilo/logo.png" alt="Logo Mini Futbol" class="logo-img">
</div>
<h1>Registrar Usuarios</h1>
  </div>

    
    <div class="registro-container">
        <form id='formUsuarios' action='<%=url%>/usuario' method="POST">
           <input type="text" name="nombre" id="nombre" placeholder="Nombre del usuario" required>
            <input type="text" name="contacto" id="contacto" placeholder="Teléfono" required>
            <input type="password" name="pass" id="pass" placeholder="Contraseña" required>
            <select name="rol" id="rol" required>
                <option value="" disabled selected>Seleccione una opción</option>
                <option value="admin">Administrador</option>
                <option value="operador">Operador</option>
                <option value="auditor">Auditor</option>
            </select>
            <input type="hidden" name="token" value="lkjrt4v3wmtiqoprmmor98">
            <button type="submit" class="btn-registrar">Crear Usuario</button>
        </form> 
    </div>

    <br>
    <form action="<%=url%>/menuGeneral" method="POST">
        <input type="hidden" name="token" value="<%=token%>">
        <button type="submit" class=" btn-volver">Volver</button>
    </form>

    <script>
        // Validación al enviar el formulario 
        document.getElementById('formUsuarios').addEventListener('submit', function(event) {
            const nombre = document.getElementById('nombre').value.trim();
            const contacto = document.getElementById('contacto').value.trim();
            const pass = document.getElementById('pass').value.trim();
            const rol = document.getElementById('rol').value.trim();

            if (!nombre || !contacto || !pass || !rol) {
                alert('Debe completar todos los datos para agregar un Usuario.');
                event.preventDefault(); // Evita que se envíe el formulario
            }
        });
    </script>
</body>
</html>